#include <stdio.h>
/**
 * main - Entry point
 * print message
 * Return: Always 0 (Success)
 */
int main(void)
{
int Alphabet;
Alphabet = 'a';
for (Alphabet = 'a'; Alphabet <= 'z'; Alphabet++)
putchar (Alphabet);
putchar('\n');
return (0);
}
