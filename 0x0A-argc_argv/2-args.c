#include "holberton.h"
#include <stdio.h>
/**
* main - a program that multiplies two numbers.
* @argc: is an argument.
* @argv: is an argument.
* Return: 0
*/
int main(int argc, char *argv[])
{
int y;
for (i = 0; y < argc; y++)
{
printf("%s\n", argv[y]);
}
return (0);
}
